first_state = normal

script {
    normal {
        on_receive_message {
            get_event_info msg message
            get_list_item temp $msg 1

            if $temp = dev_lab_kill_room
                get_list_item temp $msg 2
                if $temp = 1
                    get_mob_ids_in_region temp 1
                    get_list_item id $temp 1
                    focus_on_id $id
                    run_next_action_as focus
                    set_health 0
                end_if
            end_if
        }
    }
}
